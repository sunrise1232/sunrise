@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "nav__account", style = "display: flex; justify-content: space-between;" }))
    {
        @Html.AntiForgeryToken()

        @*<ul class="nav navbar-nav navbar-right">
            <li>
                @Html.ActionLink("Здравствуйте " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>
            <li><a href="javascript:document.getElementById('logoutForm').submit()">Выйти</a></li>
        </ul>*@

        <li class="nav__link">
            <a href="../Manage/Index">
                @User.Identity.GetUserName()
                <i class="fas fa-user" style="margin-left: 5px;"></i>
            </a>
        </li>
        <li class="nav__link">
            <a href="javascript:document.getElementById('logoutForm').submit()">
                Выйти
                <i class="fas fa-sign-out-alt" style="margin-left: 5px;"></i>
            </a>
        </li>
    }
}
else
{

    <li class="nav__link">
        <a data-modal="authorization" href="#">
            Авторизация
            <i class="far fa-user"></i>
        </a>
    </li>
    @*<li>@Html.ActionLink("Регистрация", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Выполнить вход", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>*@

}
